{% extends '::base.html.twig' %}

{% block main_content %}

	
	<fieldset>
        <legend>Confirmation de votre commande</legend>
    </fieldset>

<div class="col-md-6">
    <div class="form-group">
            <p>Nombre de bds commandées : {{cart.cartItems|length}}</p>
            <a href="{{ path('bdloc_app_cart_seelist',{id:cart.id}) }}" title="Voir la liste">Voir la liste</a><br/><br/>
            
            <div class="pointiller"></div>

            <p>Date de livraison estimée : {{cart.dateDelivery|date('d/m/Y' )}}</p>

            <p>Au point-relais : {{user.dropSpot.name}} , {{user.dropSpot.adress}}</p>

            <a href="{{ path('bdloc_app_user_updatedropspot', {'id':app.user.id}) }}" title="Changer de point-relais">Changer de point-relais</a><br/><br/>
             
            <a href="{{ path('bdloc_app_cart_validorder',{id:cart.id}) }}" title="Valider ma commande" class="btn btn-default-red ">Valider ma commande</a>
    </div>
</div>

<div class="col-md-6 ">
     <div class="col-md-6 col-md-offset-2">
        <img alt="titeuf" src="{{ asset('img/bd-perso/titeuf.jpg') }}"></a>
     </div>
 </div>

{% endblock %}